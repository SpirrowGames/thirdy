services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: thirdy
      POSTGRES_PASSWORD: thirdy
      POSTGRES_DB: thirdy
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
